The engine replayed 12 store-write-eligible events in pair-complete timestamp order: 10 blinded-quality v2 events and two automation-only negative v1 events. \texttt{buzzr-tab-unread-badge/atomic-design} finishes \texttt{suppressed} with \texttt{repository-only} discovery after 6 exact-route events. \texttt{paraform-command-menu/design-extract} finishes \texttt{suppressed} with \texttt{repository-only} discovery after 6 exact-route events. No route recovered. The six Nate rows and one legacy row remain chronology-only and created no engine route.
